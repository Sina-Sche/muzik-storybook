import { createIconElement } from "./favourites";

export default {
  title: "Components/MainplayerElements",
  parameters: { layout: "centered" },
};

export const Favourites = () => {
  return createIconElement();
};
